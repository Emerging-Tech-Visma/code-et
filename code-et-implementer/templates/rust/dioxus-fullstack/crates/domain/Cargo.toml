[package]
name = "domain"
edition.workspace = true
rust-version.workspace = true
version.workspace = true
license.workspace = true

# Dependency Rule: domain has NO workspace dependencies. Pure logic only.
[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }
# Add `time = { workspace = true }` when you introduce a value object that
# needs DateTime/Duration. The workspace already pins it.
